global:
  resolve_timeout: 5m

route:
  receiver: 'slack-notifications'

receivers:
- name: 'slack-notifications'
  slack_configs:
  # We will replace this text automatically during deployment
  - api_url: 'REPLACE_WITH_ACTUAL_WEBHOOK_URL' 
    channel: '#deployments'
    send_resolved: true
    title: '{{ if eq .Status "firing" }}🔥 Website is DOWN{{ else }}✅ Website is UP{{ end }}'
    text: >-
      {{ range .Alerts }}
        *Alert:* {{ .Annotations.summary }}
        *Description:* {{ .Annotations.description }}
        *Reason:* Check probe_http_status_code metric.
      {{ end }}